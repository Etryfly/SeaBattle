import java.util.ArrayList;

public class Game {
    private ArrayList<Ship> ships;

    public Game() {
        ships = new ArrayList<>();

    }

    public boolean checkShipInsert(Ship ship) {
        for (Ship s:
             ships) {
            if (s.isHorizontal) {

            }
        }

        return true;
    }
}
